services:
  backend:
    build: .
    container_name: la-fiorentina-backend
    restart: unless-stopped
    ports:
      - "3000:3000"
    volumes:
      - ./data:/app/data   # database SQLite được lưu ở đây trên máy host, sống sót qua các lần rebuild
    env_file:
      - .env
